## This repository contain some common use docker images

### Build docker image from docker file

``` sh
docker build -t hoangddt/myapp .
```

### Push to docker hub

``` sh
docker push $DOCKER_ID_USER/my_image
```